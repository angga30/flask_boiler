# Delman Interview Test

This api create based flask

## Build docker image
    
    sudo docker build -t flask_app .

## Run the app

    sudo docker-compose up -d

## Run the tests

    sudo docker-compose exec app flask start-test


